# Project 2 – API & Performance Test Automation

## 🔎 Project Overview

**ECI라는 클라우드 인프라 관리 사이트**를 인증, 환경, 성능을 고려해 **테스트 자동화 체계를 설계**하고, CI 환경에서 **실행 → 검증 → 리포팅까지 연결**하는 것을 목표로 한 프로젝트입니다.


## 🧠 My Role & Key Contributions

본 프로젝트에서 저는 **API 테스트 자동화와 성능 테스트 체계 설계**를 중심으로 기여했습니다.
API/성능/E2E 세 분야의 '네트워크', '병렬파일시스템' 도메인 담당으로서 공통 작업+개인작업을 진행했습니다.

특히 다음 영역에 집중했습니다.

<details>
<summary>OAuth 기반 인증 흐름 분석 및 **토큰 발급·관리 구조 설계**</summary>
- OAuth 인증 흐름을 분석하여 토큰 발급 로직을 분리
- 발급된 토큰을 .env에 저장하고 pytest fixture로 공통 사용
- 토큰 만료 시 재발급 가능하도록 구조 설계

**결과**
- API 테스트 간 인증 의존성 제거
- 테스트 재사용성과 실행 안정성 확보

</details>

- 테스트 코드와 실행 환경을 분리하기 위한 **환경 변수 표준화**
- JMeter 기반 **성능 테스트 자동화 파이프라인 구축**
- 부하 실험을 통한 **성능 한계·안정 구간 도출**
- Jenkins + Allure를 활용한 **CI 기반 테스트 결과 리포팅**
- 결과 보고서 작성

> E2E 테스트는 API 테스트의 신뢰성 확보를 우선 전략으로 두고,  
> 사용자 흐름 검증을 위한 **보조 수단으로 부분 적용**했습니다.

## 🧠 Key Contributions


### 1️⃣ OAuth 기반 토큰 발급·관리 구조 설계

- OAuth 인증 흐름을 분석하여 토큰 발급 로직을 분리
- 발급된 토큰을 .env에 저장하고 pytest fixture로 공통 사용
- 토큰 만료 시 재발급 가능하도록 구조 설계

**결과**
- API 테스트 간 인증 의존성 제거
- 테스트 재사용성과 실행 안정성 확보


### 2️⃣ 테스트 환경 변수 표준화 (.env + config.py)

- 계정 정보, ZONE_ID, JMeter 실행 경로 등을 코드와 분리
- 로컬 / CI 환경에서 동일한 테스트 코드 재사용 가능하도록 구성

**결과**
- 환경 변경 시 코드 수정 최소화
- CI 연계 용이성 향상

### 3️⃣ 성능 테스트 자동화 파이프라인 구축

- JMeter .jmx 실행을 위한 run_jmeter.sh 작성
- 결과 .jtl 파일을 검증하는 Python 스크립트 구현
- 두가지를 한번에 자동실행할 수 있는 run_all_on_local.sh 작성
- Jenkins에서 성능 테스트 자동 실행 가능하도록 구성

**결과**
- 성능 테스트 실행 및 결과 검증 자동화
- 반복 실행 가능성 확보
  

### 4️⃣ 성능 실험 설계 및 한계 구간 도출

- thread / ramp-up / loop count를 조절하며 부하 실험 수행
- 최대 부하 구간, 성능 한계 지점, 안정 구간 도출

**결과**
- 단순 실행이 아닌 실험 기반 성능 분석 수행
- 성능 기준 판단 근거 확보


### 5️⃣ 자동화 테스트 결과 리포팅 체계 구축 (Allure + CI)

- pytest + Allure 연동
- Jenkins 실행 결과를 시각화된 리포트로 제공

**결과**
- 테스트 결과를 팀 단위로 공유 가능한 산출물로 제공
